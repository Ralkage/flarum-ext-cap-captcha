version: "3.8"

services:
  cap:
    image: tiago2/cap:latest
    container_name: cap-captcha
    ports:
      - "3737:3000"
    environment:
      ADMIN_KEY: cap-admin-secret-2026
    volumes:
      - cap-data:/usr/src/app/.data
    restart: unless-stopped

volumes:
  cap-data:
